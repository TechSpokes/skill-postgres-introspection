{
  "version": "20240101000003",
  "public": {
    "tables": {
      "accounts": {
        "comment": "Billing account. One per paying customer. Users belong to an account.",
        "columns": [
          { "name": "id", "type": "uuid", "nullable": false, "default": "gen_random_uuid()", "comment": null },
          { "name": "name", "type": "text", "nullable": false, "default": null, "comment": null },
          { "name": "status", "type": "text", "nullable": false, "default": "'active'::text", "comment": "Lifecycle: active, then suspended, then closed. Only active accounts may create users. See docs/billing/account-lifecycle.md." },
          { "name": "created_at", "type": "timestamp with time zone", "nullable": false, "default": "now()", "comment": null }
        ],
        "primary-key": ["id"],
        "foreign-keys": [],
        "indexes": [
          { "name": "accounts_pkey", "definition": "CREATE UNIQUE INDEX accounts_pkey ON public.accounts USING btree (id)" }
        ],
        "row-level-security": { "enabled": false, "forced": false, "policies": [] },
        "grants": { "app_user": ["SELECT"] }
      },
      "users": {
        "comment": null,
        "columns": [
          { "name": "id", "type": "uuid", "nullable": false, "default": "gen_random_uuid()", "comment": null },
          { "name": "account_id", "type": "uuid", "nullable": false, "default": null, "comment": null },
          { "name": "email", "type": "text", "nullable": false, "default": null, "comment": null }
        ],
        "primary-key": ["id"],
        "foreign-keys": [
          { "columns": ["account_id"], "references": "public.accounts(id)", "on-delete": "CASCADE", "on-update": "NO ACTION" }
        ],
        "indexes": [
          { "name": "users_pkey", "definition": "CREATE UNIQUE INDEX users_pkey ON public.users USING btree (id)" }
        ],
        "row-level-security": { "enabled": true, "forced": false, "policies": [] },
        "grants": { "app_user": ["SELECT", "INSERT"] }
      }
    }
  },
  "extensions": { "plpgsql": "1.0" },
  "other-objects": { "sequences": 0, "materialized-views": 0, "partitioned-tables": 0, "event-triggers": 0, "default-privileges": 0 }
}
